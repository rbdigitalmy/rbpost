export function mediaPath(url:string,userId:string){const parsed=new URL(url,'https://threads-ai.invalid');if(parsed.origin!=='https://threads-ai.invalid'||parsed.pathname!=='/api/media')throw new Error('Gambar mesti dimuat naik melalui platform.');const path=parsed.searchParams.get('path')||'';if(!new RegExp(`^${userId}/[0-9a-f-]{36}\\.(jpg|png|webp)$`).test(path))throw new Error('Gambar tidak dimiliki oleh akaun ini.');return path;}
export function imageMime(bytes:Uint8Array){if(bytes[0]===0xff&&bytes[1]===0xd8&&bytes[2]===0xff)return 'image/jpeg';if(bytes.length>=8&&Buffer.from(bytes.subarray(0,8)).equals(Buffer.from([137,80,78,71,13,10,26,10])))return 'image/png';if(Buffer.from(bytes.subarray(0,4)).toString()==='RIFF'&&Buffer.from(bytes.subarray(8,12)).toString()==='WEBP')return 'image/webp';throw new Error('Fail gambar tidak disokong. Pilih JPG, PNG atau WebP.');}
